using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundStartUpScript : MonoBehaviour
{
    public SettingsMenuScript soundChange;

    // Start is called before the first frame update
    void Start()
    {
        //soundChange.StartUpdate();
    }

    
}
